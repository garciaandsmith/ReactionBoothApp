# Database (PostgreSQL — use Vercel Postgres, Neon, Supabase, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/reactionbooth"
DIRECT_DATABASE_URL="postgresql://user:password@localhost:5432/reactionbooth"

# NextAuth
NEXTAUTH_SECRET="change-me-to-a-random-secret-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Email (SMTP) — configure for your provider
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="ReactionBooth <noreply@reactionbooth.app>"

# Vercel Blob Storage (for video uploads)
BLOB_READ_WRITE_TOKEN=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
