# Database (SQLite — zero config, just works)
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="dev-secret-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Email (SMTP) — optional, emails will be logged to console if not configured
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="ReactionBooth <noreply@reactionbooth.app>"

# Vercel Blob Storage — optional, uploads stored locally if not set
BLOB_READ_WRITE_TOKEN=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"