# ─────────────────────────────────────────────────────────────────────────────
# Database — Supabase (PostgreSQL)
# ─────────────────────────────────────────────────────────────────────────────
# In Supabase: Project Settings → Database → Connection string
#
# DATABASE_URL  → "Transaction" pooler (port 6543). Used by the app at runtime.
# DIRECT_URL    → "Session" / direct connection (port 5432). Used by Prisma
#                  migrate and db push to run schema changes.
#
# In the Supabase dashboard, go to:
#   Project Settings → Database → Connection string → URI
#   Toggle between "Transaction" (pooler) and "Session" / direct modes.
#
DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# ─────────────────────────────────────────────────────────────────────────────
# NextAuth
# ─────────────────────────────────────────────────────────────────────────────
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="change-me-to-a-random-secret-in-production"

# Set to your production domain (no trailing slash)
NEXTAUTH_URL="https://your-app.vercel.app"

# ─────────────────────────────────────────────────────────────────────────────
# Email (SMTP) — Resend
# ─────────────────────────────────────────────────────────────────────────────
# 1. Sign up at https://resend.com (free — 100 emails/day)
# 2. Create an API key: Resend dashboard → API Keys → Add API Key
# 3. For testing you can use the sandbox sender (onboarding@resend.dev),
#    which works without domain verification but only delivers to your
#    own verified email address.
# 4. For real use: add and verify your domain in Resend → Domains.
#
SMTP_HOST="smtp.resend.com"
SMTP_PORT="587"
SMTP_USER="resend"
SMTP_PASS="re_xxxxxxxxxxxx"         # Your Resend API key
EMAIL_FROM="ReactionBooth <noreply@yourdomain.com>"

# ─────────────────────────────────────────────────────────────────────────────
# Vercel Blob Storage — for video uploads
# ─────────────────────────────────────────────────────────────────────────────
# In Vercel dashboard: Storage → Create Store → Blob → Connect to project.
# The BLOB_READ_WRITE_TOKEN is added to your project env vars automatically.
# Copy it here for local development if needed.
#
BLOB_READ_WRITE_TOKEN=""

# ─────────────────────────────────────────────────────────────────────────────
# YouTube cookies — required to download YouTube videos during compositing
# ─────────────────────────────────────────────────────────────────────────────
# Without this, YouTube bot-detection rejects server-side downloads with
# "Sign in to confirm you're not a bot".
#
# How to obtain:
#   1. Log in to YouTube in your browser.
#   2. Install a browser extension like "EditThisCookie" or "Cookie-Editor".
#   3. Export the youtube.com cookies as a JSON array.
#   4. Paste the JSON array (minified to one line) as the value below.
#
# The array must contain objects with at least "name" and "value" fields:
#   [{"name":"VISITOR_INFO1_LIVE","value":"xxx"},{"name":"YSC","value":"yyy"},...]
#
# In Vercel: add YOUTUBE_COOKIES as an environment variable (set to the JSON).
#
YOUTUBE_COOKIES=""

# ─────────────────────────────────────────────────────────────────────────────
# App
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
